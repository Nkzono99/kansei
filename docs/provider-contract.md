# Provider Contract

Providers supply health, project status, doctor checks, and delegation plans.

The initial implementation includes a `generic-code` provider for local git
projects. Domain-specific logic remains in providers such as runops, paperops,
and harnessops.
